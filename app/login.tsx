import React from 'react';
import { LoginScreen } from '@/screens/LoginScreen';

export default function LoginPage() {
  return <LoginScreen />;
}

export const options = { title: 'Login', headerShown: false };
